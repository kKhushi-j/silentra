{
  "entities": {
    "DeviceReading": {
      "title": "Device Reading",
      "description": "A single decibel reading from a sensor device.",
      "type": "object",
      "properties": {
        "decibel": {
          "type": "number",
          "description": "The decibel value of the noise reading."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "The server timestamp of when the reading was recorded."
        },
        "zone": {
          "type": "string",
          "description": "The zone identifier for the device (e.g., 'Front Left')."
        },
        "status": {
          "type": "string",
          "description": "The online status of the sensor device.",
          "enum": ["online", "offline"]
        }
      },
      "required": ["decibel", "timestamp", "zone", "status"]
    }
  },
  "auth": {
    "providers": []
  },
  "firestore": {
    "/devices/{deviceId}": {
      "schema": {
        "$ref": "#/backend/entities/DeviceReading"
      },
      "description": "Stores real-time decibel readings from sensor devices like Mic_A, Mic_B, etc."
    }
  }
}
